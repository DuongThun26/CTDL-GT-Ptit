#include<bits/stdc++.h>
using namespace std;

int main(){
	int t;
	cin >> t;
	while(t--){
		string s;
		cin >> s;
		stack <char> st1, st2;
		int ans = 0;
		for(int i = 0; i < n; i++){
			if(s[i] == '(') st1.push(s[i]);
			else st2.push(s[i]);
		}		
		if(st1.size() == st2.size()){
			if()
		}
	}
	return 0;
}

